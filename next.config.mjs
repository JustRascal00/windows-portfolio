/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "4kwallpapers.com",
      "wallpaperaccess.com",
      "upload.wikimedia.org",
    ],
  },
};

export default nextConfig;
